# TheoShift v4.2.0 Release Notes

**Release Date:** February 9, 2026  
**Deployment:** STANDBY (Blue) - Ready for Production  
**Feature Branch:** FB-025 Location Library

---

## 🎯 Overview

This release introduces the **Location Library** feature, enabling centralized management of event locations with Google Maps integration. Event organizers can now save frequently used locations, view them on maps, and reuse them across multiple events.

---

## ✨ New Features

### Location Library (FB-025)

**Database & API:**
- ✅ New `locations` table with fields: name, address, lat/lng, notes, isActive, usage tracking
- ✅ Foreign key relationship: `events.locationId` → `locations.id`
- ✅ CRUD API endpoints for location management (`/api/locations`)
- ✅ Search, sorting, and filtering capabilities
- ✅ Usage count tracking for popular locations

**UI Components:**
- ✅ **LocationSelector** - Smart dropdown with autocomplete, recent/popular locations, keyboard navigation
- ✅ **CreateLocationModal** - Inline location creation with Google Places Autocomplete
- ✅ **MapPreview** - Embedded Google Map with marker and directions link
- ✅ **Admin Locations Page** - Full location management interface at `/admin/locations`

**Google Maps Integration:**
- ✅ Places API autocomplete for address input
- ✅ Automatic geocoding (lat/lng population)
- ✅ Interactive map preview with markers
- ✅ Direct links to Google Maps directions

**Event Management:**
- ✅ Location selector integrated into event creation
- ✅ Location selector integrated into event editing
- ✅ Support for both saved locations and free-text entry
- ✅ Backward compatible with existing events

---

## 🔧 Technical Changes

### Database Schema
```sql
-- New locations table
CREATE TABLE locations (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  address TEXT,
  latitude DOUBLE PRECISION,
  longitude DOUBLE PRECISION,
  notes TEXT,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  created_by TEXT,
  usage_count INTEGER DEFAULT 0
);

-- Foreign key added to events table
ALTER TABLE events ADD COLUMN location_id TEXT REFERENCES locations(id);
CREATE INDEX idx_events_location_id ON events(location_id);
```

### Prisma Schema Updates
- Added `locations` model with proper field mappings
- Added `locationRef` relation to `events` model
- Configured `@map` directives for snake_case database columns

### API Endpoints
- `GET /api/locations` - List all active locations with search/filter
- `POST /api/locations` - Create new location
- `GET /api/locations/[id]` - Get location details
- `PUT /api/locations/[id]` - Update location
- `DELETE /api/locations/[id]` - Soft delete location
- `PUT /api/events/[id]` - Updated to support location relations

### Environment Configuration
- Added `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` for client-side Maps integration
- API key configured on STANDBY server

---

## 🐛 Bug Fixes

### Prisma Relation Handling
- **Fixed:** Prisma relation field handling for `locationId`
  - Issue: Direct foreign key updates were rejected by Prisma runtime
  - Solution: Implemented proper `connect`/`disconnect` pattern for relations
  - Impact: Event updates with location selection now work correctly

### Field Casing Corrections
- **Fixed:** Volunteer overseer field names to match database schema
  - Changed: `volunteerOverseerName` → `volunteeroverseername`
  - Changed: `volunteerOverseerPhone` → `volunteeroverseerphone`
  - Changed: `volunteerOverseerEmail` → `volunteeroverseeremail`
  - Changed: `volunteerOverseerAssistants` → `volunteeroverseerassistants`
  - Impact: Event updates no longer fail with "Unknown argument" errors

### Admin Navigation
- **Fixed:** Location Library menu item visibility in admin sidebar
- **Fixed:** Build cache issues preventing new code from deploying

---

## 📝 Implementation Notes

### Backward Compatibility
- ✅ Existing events continue to work with free-text location field
- ✅ `location` field preserved for display purposes
- ✅ `locationId` is optional - events can use either saved locations or free text
- ✅ No data migration required

### Google Maps API
- API Key: `AIzaSyBzduP5waHOaPMkB_e2lK4CWRyvb4HKnqc`
- Services enabled: Maps JavaScript API, Places API, Geocoding API
- Configured on both STANDBY and LIVE servers

### Deployment Strategy
- Database changes applied to both STANDBY and LIVE (backward compatible)
- Application code deployed to STANDBY for testing
- Ready for traffic switch via `/release` workflow

---

## 🧪 Testing Performed

### Automated E2E Tests (qa-01 → STANDBY)
**Test Date:** February 9, 2026  
**Test Environment:** qa-01 → https://green.theoshift.com (STANDBY)  
**Test Framework:** Playwright  
**Results:** ✅ **52 passed, 1 skipped** (98% pass rate)

**Test Coverage:**
- ✅ UI Modernization (Volunteers & Positions pages)
- ✅ Event creation and management
- ✅ Position management workflows
- ✅ User management
- ✅ Navigation and routing
- ✅ Mobile responsiveness
- ✅ Performance benchmarks
- ✅ Critical smoke tests
- ✅ Date display verification
- ✅ Event permissions UX

**Test Execution Time:** 1.6 minutes  
**Test Runner:** qa-01 (centralized E2E testing container)  
**Target:** STANDBY environment (green.theoshift.com)

### Manual Testing on STANDBY
- ✅ Create new location with Google Maps autocomplete
- ✅ Select saved location from dropdown
- ✅ Edit event and change location
- ✅ View location on map preview
- ✅ Admin locations page functionality
- ✅ Search and filter locations
- ✅ Usage count tracking
- ✅ Existing events load without errors

### Verified Scenarios
1. **New Event with New Location:** Create event → Use autocomplete → Save location → Event created
2. **New Event with Saved Location:** Create event → Select from dropdown → Event created
3. **Edit Event Location:** Edit existing event → Change location → Save successfully
4. **Admin Management:** View all locations → Search → Edit → Deactivate
5. **Map Integration:** View location details → See map → Click directions → Opens Google Maps

---

## 📊 Database Impact

### New Records
- 0 locations pre-populated (users will create as needed)
- 0 existing events modified (backward compatible)

### Performance
- New index on `events.location_id` for efficient lookups
- Location search uses text matching on name/address fields
- Usage count updated on each event save (minimal overhead)

---

## 🚀 Deployment Checklist

- [x] Database schema updated on STANDBY
- [x] Database schema updated on LIVE (backward compatible)
- [x] Google Maps API key configured on STANDBY
- [x] Google Maps API key configured on LIVE
- [x] Application code deployed to STANDBY
- [x] Manual testing completed on STANDBY
- [x] Automated E2E tests passed (52/53 tests)
- [x] Admin menu item verified
- [x] Existing events verified to load correctly
- [x] New location creation verified
- [x] Event editing with location selection verified
- [ ] Traffic switch to STANDBY (ready for `/release`)

---

## 📚 Documentation

### User Guide
- Location Library accessible at `/admin/locations`
- Create locations during event creation or from admin page
- Reuse saved locations across multiple events
- View locations on interactive maps
- Track which locations are most popular

### Admin Guide
- Manage all locations from admin interface
- Search and filter by name or address
- Deactivate unused locations (soft delete)
- View usage statistics
- Edit location details and coordinates

---

## 🔄 Next Steps

1. **Traffic Switch:** Run `/release` to switch traffic from GREEN to BLUE
2. **Monitoring:** Watch for any location-related errors in production logs
3. **User Training:** Notify event coordinators about new Location Library feature
4. **Future Enhancements:**
   - Bulk import locations from CSV
   - Location categories/tags
   - Distance calculations between locations
   - Location history/audit trail

---

## 👥 Contributors

- **Development:** Cascade AI
- **Testing:** Cory Allen
- **Deployment:** Automated via blue-green deployment workflow

---

## 📞 Support

For issues or questions about the Location Library feature:
- Check admin logs at `/admin/audit-logs`
- Review location API responses in browser DevTools
- Contact system administrator

---

**Status:** ✅ Ready for Production Release  
**Next Action:** Run `/release` workflow to switch traffic to STANDBY
